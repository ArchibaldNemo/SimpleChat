import template from './contactList.html';
import controller from './contactList.controller';
import './contactList.scss';

let contactListComponent = {
  bindings: {},
  template,
  controller
};

export default contactListComponent;
